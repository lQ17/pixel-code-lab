# Pixel Code Lab 开发交接

更新：2026-09-22。目录：`D:\pixel-code-lab`，Windows / PowerShell。

## 当前状态与边界

面向学生的浏览器 Python 像素编程闯关游戏，学生编写 `pixel(x, y)` 返回颜色编号。React / TypeScript / Vite、Monaco、Pyodide Worker、三关挑战、联动画布和本地存档均已实现；已完成相对移动原点，并新增 Python 3D 体素创作、上下联动视图、剖切、悬停查询及七个三维目标的匹配判定和通关记录。

- 当前分支沿用 `main`；上一轮二维挑战积木提交为 `a4b98be`，页面重构基线为 `96b6d09`。本轮三维积木开发未授权提交，不自动执行 Git 操作。
- 用户要求：“接下来不必每次都 git，有我的指示再 git。”不要例行执行 Git 操作；一次提交授权不延续到下一次，不自行推送或部署。
- 三维已实现七关稳定 ID 配置、挑战/自由创作拆分及本地作品库。二维挑战原有规则继续保留；三维两种玩法均保留显式示例载入。账号、云存档、AI、C++ 和关卡编辑器尚未纳入本阶段；积木现已覆盖二维/三维的自由创作及现有挑战，详见末尾三维积木章节。主要环境为 Windows 桌面浏览器。
- 开发约束统一入口为根目录 `AGENTS.md`；开始开发先阅读本交接文档。
- 历史过程查 `docs/开发记录.md`；旧规划和早期记录不代表当前待办。

## 三维创作原型（2026-09-18）

- 总体路线见 `docs/总体规划.md`；旧 V0.1 规划中的“不做 3D / 自由创作”仅适用于当时版本。用户已将此前测试验收视为通过。
- 顶部切换二维挑战和“3D 体素”；三维左侧切换“挑战 / 自由创作”，首次默认自由创作，之后恢复上次玩法。三维为固定 17×17×17 空间，x/y/z 均为 -8～8，Y 向上；`voxel(x, y, z)` 返回严格整数 0～8，0 为空。三维暂不支持 move_origin。
- 七种目标均提供可显式载入的示例，载入前对已有代码做覆盖确认。拖动旋转、滚轮缩放、方向键旋转、+/- 缩放，可切换坐标辅助和重置视角。坐标辅助包含完整空间立方体线框、贴底边的 X/Z 刻度、贴竖边的 Y 刻度，以及用户最新要求恢复的中心三色虚线方向轴。它们随模型旋转缩放，地面网格仍移除；密集时减少数字标签，端视退化的边缘轴暂不绘制。
- 共用 Runner，模式切换取消执行；失败保留历史作品。三维数据按 z 升序、y 降序、x 升序排列。Canvas 绘制正交投影方块表面，剔除内部面并按深度绘制，无额外三维依赖。
- schemaVersion 仍为 1，可选三维字段见下方存档与玩法说明。三维挑战各关代码、自由创作草稿与二维各关独立；自由创作新增本地作品库，保存命名代码及参考选择。渲染结果和视角不持久化，刷新后重新运行。
- 新增入口：`src/engine/voxel.ts`、`src/renderers/VoxelCanvas.tsx`、`tests/voxel.spec.ts`。
- 本阶段验证：lint / build 通过；新增 Edge 端到端测试通过（示例数量、旋转重置、报错、返回值、存档、超时、停止、切模式取消及回到二维运行）；单独核验三维数组坐标顺序；已查看 1440×900 立方体、球体截图。未重跑旧测试全集。

## 三维剖切

- 中心轴 X/Y/Z 圆形手柄可拖动，每次按整数层调整上界，保留坐标 ≤ 上界的体素；三轴可组合。手柄支持方向键、Home/End。
- 隐藏体素保留淡色点状提示，重新计算截面外露面；不修改原始 colors、代码、运行结果或存档。显示数量另列在剖切状态中。
- “恢复完整模型”恢复全部三轴，“重置视角”只恢复相机。关闭坐标辅助隐藏手柄但保留剖切，并可恢复完整模型。
- 剖切、相机和坐标辅助状态由 App 的 useVoxelControls 共享，在本次页面会话中跨模式切换保留，刷新后重置，不持久化。端视投影过短的手柄不显示，旋转后可操作。

## 必须保留的行为（二维挑战）

- 三关全部开放，按最终像素图判断，不限制解法；完全一致才通关，通关后不自动跳关，后续失败不撤销历史通关。
- 匹配率只统计“目标或结果至少一方不为空”的格子；双方都空不计，集合为空为 100%。非完全匹配最多显示 99.9%。颜色 0 是空白，不是白色。
- 系统逐格调用 `pixel(x, y)`，返回值必须严格为 0～8 的 Python int，排除 bool、float、None。每关首次模板仅含 `def pixel(x, y):` 和 `return 0`，不能覆盖已有代码。
- 默认原点居中，x 向右、y 向上，整数代表格子中心；关卡尺寸固定。目标规则和颜色配置以 `src/engine/levels.ts` 为准，不向学生展示目标公式或错误格定位。
- 左侧代码，右侧上方目标、下方作品；沿用科幻终端风格。画布使用整个矩形视口等比例缩放，格子保持正方形，缩放和平移两图联动。
- 目标图右上角保留“坐标系”和“重置视图”。边缘轴位于矩阵左侧/下方；标准轴在当前原点相交，原点只显示一个 0，x/y 标识位于矩阵外侧正方向箭头旁。X 红、Y 绿。
- 悬停提示位于光标右上方，显示坐标、真实色块和颜色名称；移出格子隐藏。匹配率和历史结果位于作品右上角浮层，不挤占画布。不恢复已删除的画布引导文字。

## move_origin(dx, dy)

在 `pixel()` 外、逐格计算前调用，参数为严格整数；正数向右/向上，多次调用相对累加。每次运行从默认原点重新开始。例如先调用 `move_origin(2, 3)`，再调用 `move_origin(-1, 0)`，最终原点位于初始坐标 `(1, 3)`。

- 执行层调用 `pixel(x - ox, y - oy)`；目标像素位置、数组顺序和实际格子匹配规则不变。允许原点移出矩阵。
- 成功运行后，两图的刻度、标准轴位置和悬停坐标同步更新；边缘轴仍留在矩阵边缘。超出视口的轴裁切。
- 编辑、失败或停止时保留历史作品及其原点；每关会话内保存各自作品与原点，无作品时使用默认原点。重置视图只重置缩放和平移。
- 逐格执行期间调用会报 RuntimeError，参数类型或累计偏移超出实现支持的整数范围会报错，并沿用用户代码行号定位。

## 实现入口与注意点

数据流：App → PythonRunner → Worker → Pyodide / execute.py → `colors[]`、`origin` → App 判定并同步两图。

| 文件 | 职责 |
| --- | --- |
| `src/App.tsx`、`src/App.css` | 界面、运行状态、每关历史作品及原点 |
| `src/runners/` | Worker 生命周期、Python 执行、move_origin、结果协议 |
| `src/engine/levels.ts`、`evaluate.ts` | 关卡配置、目标生成、匹配判定 |
| `src/engine/view.ts`、`src/renderers/PixelCanvas.tsx` | 联动视图、坐标命中、轴和像素绘制 |
| `src/engine/voxel.ts`、`src/renderers/VoxelCanvas.tsx`、`src/hooks/useVoxelControls.ts` | 三维示例/参考数据、渲染、悬停、双图视角与剖切共享状态 |
| `src/hooks/useProgress.ts` | 本地存档、校验和保存异常 |
| `src/components/CodeEditor.tsx`、`HelpDialog.tsx` | 编辑器、错误标记、使用说明 |
| `tests/game.spec.ts`、`playwright.config.ts` | 二维测试及 Edge 测试配置 |
| `tests/voxel.spec.ts`、`voxel-cut.spec.ts`、`voxel-hover.spec.ts`、`voxel-score.spec.ts` | 三维执行、剖切、悬停、目标判定与通关测试 |

- 数组按原始 y 从 +radius 到 -radius、每行 x 从 -radius 到 +radius 排列，目标、执行、渲染必须一致。Python 内部遍历，不由 JS 逐格调用。
- 执行限时 2 秒、初始化限时 60 秒；停止/超时重建 Worker，切关取消运行，保留任务编号与 generation 防止旧结果污染。错误行号取自 student.py，不能伪造。Worker 不是完整恶意代码沙箱。
- 存档键 `pixel-code-lab.progress`，schemaVersion=1；保存代码、历史通关、关卡、说明状态及可选 mode / voxelCode / voxelExample / voxelPassed / voxelActivity / voxelLevelId / voxelCodes / voxelReferenceId / voxelProjects / voxelProjectId / voxelDraftName / pixelChallengeEditors / pixelChallengeBlocks，不持久化渲染数组、原点或视图。刷新后重新运行。损坏存档先保留，不能擅自覆盖；改关卡 ID 或 schema 需考虑迁移。
- 浏览器来源不同会隔离存档，尽量沿用已有地址和端口。Python 与 Monaco 由项目本地提供资源。

## 运行与验证

依赖版本以 package-lock.json 为准；已有依赖无需重装。日常运行 `npm run dev`；检查使用 `npm run lint`、`npm run build`、`npm test`。

`npm test` 会构建并启动 `127.0.0.1:4173`，端口被占用时先确认进程，不要终止无关服务。最近开发预览为 http://localhost:5173/；历史曾使用 4174。preview 不监听源码，修改后需重新构建；端口与服务是否仍运行需现场确认。

上一轮（2026-09-19）验证：lint/build 通过；Playwright 全量回归 18/18 项通过，包含原有二维、三维测试和新增玩法/存档/取消测试。随后修正新存档默认挑战选择被创作参考影响的边界，补充断言并复测玩法测试 3/3 通过，lint/build 再次通过。已查看 1440×900 挑战与创作、1180×768 挑战截图，按钮、编辑器与双图布局正常。未执行 Git 提交。

测试注意：Monaco 隐藏 textbox 不宜直接 fill；`keyboard.insertText` 可能叠加缩进，验证真实粘贴应使用剪贴板 + Ctrl+V，不要为模拟输入关闭正常缩进。错误断言使用 `.error[role=alert]`，悬停位置按画布 boundingBox 计算。修改界面后检查实际截图，修改逻辑后运行相关测试并更新开发记录。

## 2026-09-19 更新：三维双视图

三维右侧上方为当前目标或参考模型，下方为当前运行作品；两图共用 useVoxelControls 的旋转、缩放、剖切与坐标辅助状态。参考数据入口 voxelReference；挑战选择由 voxelLevelId 保存，创作参考现由 voxelReferenceId 保存，旧 voxelExample（0～2）按固定的立方体/球体/彩色阶梯映射迁移。统一控制按钮在上图，两幅图均可旋转或拖动剖切手柄。参考图不受用户代码改动影响，点击左侧卡片切换，不自动载入代码。

## 三维悬停查询

上下视图均有白色粗边体素高亮及光标右上坐标/颜色提示。按投影面内命中和实际交点深度查询最前方未剖切体素，显示原始颜色。浮层沿用二维样式增加 Z 坐标。视图变化清除旧提示。

## 二维悬停高亮

二维目标图与作品图的悬停格（包括空白格）增加白色 2.5px 边缘，使用与坐标弹窗相同的 hitCell 命中，移出同步隐藏。


## 三维目标判定与通关（2026-09-19）

- 现有立方体、球体、彩色阶梯参考目标新增完整体素匹配率和独立历史通关记录，沿用 evaluate 的严格颜色比较和非空并集规则。剖切、缩放、旋转不影响判定，内部差异也必须比较。
- 初次判定阶段只保留示例载入方式；后续已拆分独立挑战/创作入口和挑战分关代码，见下节。本轮仍未新增关卡。
- 目标稳定 ID 为 voxel-cube / voxel-sphere / voxel-stairs，成功运行作品与成绩按目标隔离；切目标取消旧运行。失败保留该目标上次成功结果，后续不匹配不撤销历史通关，不自动切换目标。
- 存档 schemaVersion=1 增加可选 voxelPassed，兼容缺少该字段的旧存档；二维 passed 不混入三维记录。顶部进度仅在挑战玩法显示。刷新恢复历史通关，当前作品需重新运行。
- 新增验证入口 tests/voxel-score.spec.ts。

## 三维挑战与自由创作拆分（2026-09-19）

- 本轮用户授权完成三维分关代码和玩法拆分，沿用三个目标。保留用户刚精简的界面，不恢复已删说明、微标签、手势提示和页脚文案。
- 挑战代码存入 voxelCodes，以 voxel-cube / voxel-sphere / voxel-stairs 为键，当前关卡由 voxelLevelId 保存。首次未保存的关卡使用空模板，不自动载入示例；切关保留各自代码、会话作品与成绩。
- 创作继续使用独立 voxelCode；参考选择于 2026-09-21 改为 voxelReferenceId（兼容旧 voxelExample）；切参考只改变上图，不替换代码或下方作品。创作不计算成绩、不显示匹配率与通关进度，也不更新 voxelPassed。
- voxelActivity 为 challenge / create，缺省 create。两种三维玩法均保留编辑器的“载入示例”按钮；覆盖已有非初始代码需确认，取消不修改代码。恢复模板只影响当前关卡或创作代码。
- 旧存档缺少 voxelCodes 时，将 voxelCode 复制到旧 voxelExample 对应的挑战关卡，同时保留创作原代码；已有 voxelCodes（含空对象和空字符串）不再次迁移。新存档尚未选择挑战时默认立方体，不受创作参考选择影响。旧通关标记完整保留，其余新字段严格校验，损坏存档沿用显式恢复流程。
- 切三维玩法、关卡或创作参考时取消旧运行并清理当前错误；任务编号继续隔离过期结果。创作作品与各挑战作品分开，失败和取消不替换上次成功作品；旋转、缩放和剖切仍跨玩法共享。
- 验证入口新增 tests/voxel-modes.spec.ts，现有三维测试改为显式选择参考及载入示例；成绩测试进入挑战玩法。

## 三维七关与稳定 ID 配置（2026-09-21，当前）

- 三维挑战扩充为七关，显示顺序为立方体、空心立方体、圆柱、球体、彩色阶梯、金字塔、简单房屋。全部开放，不自动跳关；挑战和自由创作均提供七个目标/参考及显式示例。
- `src/engine/voxel.ts` 使用按稳定 ID 索引的 `voxelLevels`，每项包含名称、目标函数和可选 `exampleCode`。无示例配置时不显示载入按钮。`voxelReference(id)` 按 z 升序、y 降序、x 升序生成数组。旧三个 ID 和目标数据保留。
- 新 ID：voxel-hollow-cube / voxel-cylinder / voxel-pyramid / voxel-house。对应体素数为 386 / 441 / 455 / 470。空心立方体为紫色闭合单层外壳；圆柱绿色实心；金字塔黄色实心；房屋黄色实心主体、红色屋顶和正面蓝色门窗（门窗为着色体素，不是开口）。
- schemaVersion 保持 1，新增可选 voxelReferenceId 用于创作参考；旧 voxelExample 只保留兼容读取，0/1/2 固定映射原立方体/球体/阶梯，不依赖新列表位置。新 ID 字段优先。旧分关代码缺失时仍仅迁移一次；已有分关代码、空字符串和历史通关保留，新关默认空模板。
- 左侧三维卡片压缩为七项布局，空间不足时列表独立滚动。二维行为不变。
- 本轮实测：`npm run lint`、`npm run build` 通过，`npm test` 全量 22/22 通过（含 4 项新增测试，覆盖目标边界/体积/内腔、真实 Pyodide 执行层逐格对照、旧存档迁移与新 ID 往返、七关界面及玩法隔离）。生产构建仍有既有 Monaco 资源体积提示。
- 用户已完成七关人工交互与目视验收，随后按指示提交 `3283009`。该验收不覆盖后续本地作品库。

## 三维本地作品库（本轮）

- 入口为「3D 体素 → 自由创作」代码工作台中的「作品库」「保存作品」。作品库弹窗包含命名保存、更新原作品、另存副本、新建草稿、列表打开、导入/导出；不新增删除、账号或云端功能。
- `voxelCode` 仍作为当前草稿自动保存，新增 `voxelDraftName` 保存未提交的名称；`voxelProjectId` 关联正在编辑的作品，空值表示独立草稿。`voxelProjects` 保存独立 ID、名称、代码、参考模型 ID、创建及更新时间，不包含运行结果。原有旧存档缺少这些字段时视为空作品库，旧代码原样保留。
- 修改草稿不自动覆盖已保存作品，必须点击「保存作品」；另存副本使用新 ID 并切换关联，保留原作品。同名新作品及导入自动添加编号；更新已有作品时重名会提示改名。列表按更新时间倒序。
- 打开或新建前，若草稿代码、名称或参考选择与已保存作品不同，则提示确认。取消无变化；确认后将当前内容保留为独立「（草稿）」作品，再切换。备份和切换作为一次存档写入，保存失败不切换、不替换原作品。成功切换取消旧运行，清空创作画面和错误，打开后手动重新运行；挑战作品不受影响。
- 作品格式为 JSON，format=`pixel-code-lab.voxel-project`、version=1、mode=3d、language=python、radius=8；文件不含本地 ID，导入总是新建 ID。导入仅新增列表项，不自动打开或执行。导出当前作品包含未保存到作品库的最新编辑；列表导出则使用该作品的已保存版本。
- 名称 1～80 个字符，作品代码最多 200000 个字符，导入/导出文件最多 1 MB。校验语言、维度、空间、参考、字段类型及时间；非法文件不修改存档。作品库字段损坏沿用保留原存档与显式恢复流程；读取存储失败时同样阻止自动写入，避免覆盖未读到的旧数据。存储失败仍可导出当前草稿。
- 实现入口：`src/engine/projects.ts`、`src/hooks/useProjectLibrary.ts`、`src/components/ProjectLibrary.tsx`；`useProgress.commit` 先落盘再更新界面，使用最新草稿合并以免丢失防抖期间的修改。
- 本轮验证：`npm run lint` 无错误或警告，TypeScript 与生产构建通过；最终 `npm test`（Edge）全量 30/30 通过。含 8 项作品库测试，覆盖协议、旧数据、保存/副本/刷新、未保存草稿保护、导入导出、配额错误、损坏作品库及读取失败。保留既有 Monaco 大资源提示。
- 验收结果：在 http://localhost:5173/ 实际操作自由创作入口、作品命名保存、刷新恢复草稿、更新原作品、另存副本、原作品/副本代码隔离、未保存修改确认并生成草稿副本、挑战与创作隔离及作品库弹窗布局；导出按钮显示“已生成作品文件”。浏览器下载/文件选择器不便在当前自动化浏览器中读取，因此导入、重复导入、无效文件、存储读写失败由 Windows Edge Playwright 全量测试覆盖。
- 最终验证：`npm run lint` 无错误或警告，TypeScript 与生产构建通过，`npm test` 全量 30/30 通过。用户验收产生的本地示例作品未删除，避免擅自清理浏览器存档。已提交 `ece5f6a`。

## 二维自由创作、作品预览与 PNG（2026-09-21）

- 顶部「2D 像素」下可切换挑战/自由创作，旧存档默认挑战。创作固定 21×21，使用 pixel(x,y) 和 move_origin；独立草稿、会话作品及参考选择，不计算成绩或更改挑战记录。挑战仍不展示示例或解法。
- 创作上图提供彩色十字、菱形花纹、像素小树三个独立参考。选择只切参考，显式载入示例才替换代码且保留覆盖确认。两图保持联动和当前成功作品的原点。
- 二维作品库与三维共用 useProjectLibrary，但分别保存和显示作品。二维支持命名保存、更新、另存副本、新建、打开、JSON 导入导出，沿用草稿备份和先落盘后切换；打开后仍须手动运行。
- schemaVersion 保持 1，新增可选 pixelActivity / pixelCode / pixelReferenceId / pixelProjects / pixelProjectId / pixelDraftName。旧三维字段及作品 JSON version=1 不变；二维文件 format=pixel-code-lab.pixel-project、mode=2d、radius=10。导入限制为当前维度，不自动打开或执行。
- 两种作品可选 preview 字段保存成功运行的颜色快照（二维 441 字符、三维 4913 字符，仅 0～8）。只记录与作品代码一致的结果；保存未运行或运行失败的新代码时不沿用旧图。无预览的旧作品显示「待生成预览」，打开、运行、保存后补齐；无需执行代码即可显示已有预览。缩略图自动去除外围空白以便识别。
- 创作工具栏「导出 PNG」导出当前代码的成功结果，未运行、运行中或历史结果时禁用。作品库各项 PNG 使用保存的预览，刷新后也可直接导出。透明背景、无坐标/高亮/控件；二维保持完整 21×21、840×840 像素，三维为完整模型固定视角、1024×1024，不受当前剖切/旋转影响。0 透明，7 白色、8 黑色。
- 新入口 src/engine/pixelCreation.ts、src/renderers/projectImage.ts；三维绘制与导出共用 voxelGeometry.ts 投影及面定义。作品保存只增加紧凑预览，不持久化工作区视角或运行状态。
- 本轮最终验证：npm run lint 无错误或警告，TypeScript 与生产构建通过；Windows Edge 全量回归 36 项中 35 项直接通过，1 项旧按钮名称定位失败。同步该定位后定向复测 tests/voxel-score.spec.ts 2/2 通过，36 项均已覆盖验证（未再次重复全量）。保留既有 Monaco 大资源构建提示。

## 二维积木编程原型（2026-09-22）

- 仅「2D 像素 → 自由创作」新增 Python / 积木切换，二维挑战及三维保持原行为。积木和手写代码各有独立草稿、名称、参考选择、作品关联与会话结果，切换取消旧运行，不自动覆盖或反向转换。
- Blockly 13.3.0 按需加载，中文工具箱和深色工作区。唯一不可删除/复制的像素入口；支持坐标、整数、颜色、加减乘/取余、绝对值、比较、逻辑、如果/否则和返回颜色。支持拖拽、缩放、删除、撤销、适应积木；没有循环、普通除法、变量、自定义函数和移动原点积木。
- src/blocks/model.ts 独立校验 Blockly JSON 并生成 Python，固定函数入口和末尾 return 0。严格区分数值与条件插槽，缺少表达式输入或有游离积木时提示并禁止运行；空语句分支生成 pass。仍通过 PythonRunner 执行，保留真实错误行号，并用行号映射高亮对应语句积木。Python 对照为只读展开区。
- src/blocks/examples.ts 提供与三个创作参考一致的显式积木示例，替换前确认。参考选择与载入示例分开，不向二维挑战提供答案。
- schemaVersion 仍为 1：新增可选 pixelEditor=python/blocks（缺省 python）及 pixelBlocks={document,name,referenceId,projectId}。手写 Python 原字段不变。pixelProjects 同时保存两种编辑来源，积木作品有 editor=blocks 与 blocks 文档。
- 积木文档 version=1，内含 Blockly workspace JSON；最多 250 块、结构深度 40、250 KB，数值限定严格整数 -1000000～1000000。加载前校验字段/块类型白名单、ID、连接类型、入口和资源限制；未知版本或损坏结构沿用保留原存档及显式恢复流程。
- Python 作品文件仍为 version=1；二维积木作品导出 version=2、language=python。导入总是新建 ID，不执行或自动打开。积木代码由结构重新生成，外部不一致代码不被执行，也不沿用其预览。
- 作品库支持积木命名保存、副本、打开、新建、JSON、缩略图及 PNG。可保存尚未拼完的积木结构，此时没有可运行源码和预览。跨编辑方式打开作品会保护源草稿及将被覆盖的目标草稿，确认后先备份再切换，写入失败不切换。
- 「复制为 Python 作品」在作品库新增独立 Python 副本，不切走、不修改手写草稿或原积木。打开该副本遵循正常草稿保护。已保存预览与运行代码对应，修改后失败/不完整不会伪造新作品。
- 实现入口：src/blocks/、src/components/BlocksEditor.tsx；测试 tests/blocks.spec.ts。本轮新增 Blockly 独立资源包，生产构建有 Blockly 与既有 Monaco 大资源提示；首次进入积木编辑方式才加载 Blockly。
- 超出结构限制时保留最后有效存档及当前可撤销的工作区，阻止当前草稿保存及编辑器/玩法/作品切换；先撤销或修正后继续，避免误保存旧结构。
- 本轮 lint、类型检查和生产构建通过；Windows Edge 全量 43/43 通过。随后补充结构异常保护及取消测试，重新构建并定向复测积木测试 8/8 通过，未再次重复全量。已检查 1440×900、1180×768 页面截图。
- 用户确认可用后，于 2026-09-22 按指示提交 `afea7f4`，未推送；详细验证记录见开发记录。


## 页面重构实施（2026-09-22，已实施完成）

- 严格遵循 `docs/页面重构实施方案.md`，完成入口页与精简三区域工作台重构。
- 架构与路由：采用 Hash 路由（`#/start/*` 与 `#/work/*`），由 `src/navigation/route.ts` 统一解析与序列化，非法 URL 自动修正回退合法入口页；支持浏览器前进后退与刷新恢复上下文。
- 独立入口页：`src/pages/StartPage.tsx`，支持二维/三维与挑战/自由创作快速切换，全量展示 2D 三关、3D 七关卡片（全部开放），自由创作大卡片与「继续上次工作 →」快捷跳转。
- 精简工作台：`src/pages/WorkspacePage.tsx`，视觉仅突出三大核心区域（左代码/积木 48%，右上目标/参考，右下作品/结果 52%）；移除常驻关卡侧栏与大运行面板。
- 顶栏菜单栏：`src/components/WorkspaceMenuBar.tsx`，提供「作品▾」（仅创作）、「编辑▾」、「参考▾」、「视图▾」、「运行▾」、「帮助▾」六个紧凑下拉菜单；支持未保存徽章、运行状态切换、键盘 Esc/外部点击关闭及快捷键说明。
- 辅助能力抽屉化：调色板、程序输出/日志、积木 Python 对照改为非模态抽屉或浮层，按需展开不常驻挤占主视口。
- 状态与执行保护：工作台离开时递增 generation 并取消未完成运行；积木存在结构错误时阻止切换路由离开；草稿自动保存防抖并在离开前落盘。
- 本轮验证：`npm run lint` 无错误无警告；`npm run build` 通过；新增 `tests/navigation.spec.ts` 与 `tests/workspace-layout.spec.ts`，全量 12 组端到端测试（共 56 项）全量通过。已提交 `96b6d09`。

## 重构收尾与二维挑战积木（2026-09-22，已提交）

- 本轮范围为用户确认的「重构收尾 + 二维挑战积木」；没有扩充关卡或接入三维积木。
- 二维三个挑战在工作台「编辑」菜单中可切换 Python / 图形积木。每关默认 Python；积木首次为空入口，不从自由创作复制，不载入任何答案示例。支持查看自己生成的 Python、恢复初始积木与适应工作区；挑战中不出现作品库或「复制为 Python 作品」。
- 每关两种编辑方式分别保存草稿与会话结果，按关卡记忆编辑方式；两种方式共用原 `passed` 记录及最终像素严格判定，不自动跳关、不撤销历史通关。失败保留当前编辑方式的历史作品，刷新后重新运行。
- 存档 schemaVersion 仍为 1，新增可选 `pixelChallengeEditors`（关卡 ID → python/blocks）及 `pixelChallengeBlocks`（关卡 ID → 版本化积木文档）。保留原 `codes`、`passed` 和自由创作 `pixelEditor` / `pixelBlocks`，不迁移或覆盖它们。缺少新字段的旧存档按空映射读取，未知编辑方式、非法关卡键或损坏积木沿用保留原数据和显式恢复流程。
- 切编辑方式、关卡与玩法继续取消旧执行并隔离过期结果；积木超限或结构不可保存时同时拦截菜单导航及 Hash 导航。未拼完整但合法的积木可保存与恢复，禁止运行。
- 修复三维自由创作「参考」菜单选中标记误用挑战 ID；使用说明根据维度、玩法及工作台编辑方式展示，更新调色板、运行和作品操作入口。恢复重构时丢失的真实错误行号 → 对应积木高亮；修改积木后清除旧高亮。
- 新增验证入口 `tests/challenge-blocks.spec.ts`，扩充 `tests/workspace-layout.spec.ts`。本轮验证结果见开发记录；已按用户指示提交 Git，未推送远端。
- 本轮最终实测：`npm run lint`、TypeScript 与生产构建通过，Windows Edge `npm test` 全量 65/65 通过。已查看 1440×900、1180×768 挑战积木及三维创作帮助截图。构建仍有既有 Blockly / Monaco 大资源提示。

## 三维积木（2026-09-22，本轮）

- 「3D 体素 → 挑战 / 自由创作 → 编辑 → 图形积木」均已接入现有 Blockly 编辑器。三维固定一个不可删除/复制的「决定体素颜色（x，y，z）」入口，坐标积木提供 x/y/z；运算、条件、颜色与二维共用，生成 `voxel(x, y, z)` 并通过原 PythonRunner 执行。仍无循环、变量、自定义函数和移动原点积木。
- 关卡仍为原七关，不新增目标。每关首次默认 Python，积木为空入口；切目标不自动载入内容。挑战与创作均通过「编辑 → 载入示例」显式载入七种模型的积木版本，替换前确认。所有七种示例与原目标完整体素数据一致，判定不受旋转或剖切影响。
- 三维挑战分关、分编辑方式保存草稿与会话结果，两种方式共用原 `voxelPassed`，不自动跳关、不撤销历史通关。创作两份草稿、名称、参考选择和作品关联独立；切编辑方式不会覆盖、转换或运行另一份草稿。切换继续取消运行并隔离过期结果，失败保留对应历史作品。
- schemaVersion 仍为 1，新增可选 `voxelEditor`、`voxelBlocks={document,name,referenceId,projectId}`、`voxelChallengeEditors`、`voxelChallengeBlocks`。保留旧 `voxelCode` / `voxelCodes` 及所有二维字段。旧存档默认 Python；损坏、维度不符或未知版本的新字段仍保留原存档并要求显式恢复。
- 二维积木结构 version=1 不变；三维结构 version=2、mode=3d，使用独立的 voxel_entry / voxel_coord 类型。加载时按维度验证入口、坐标类型与原有 250 块/40 层/250 KB 限制，拒绝混用二维/三维结构。编译器保持真实错误行号与对应语句积木高亮。
- 三维作品库支持积木保存、另存副本、打开/新建、JSON、预览、PNG 和「复制为 Python 作品」。三维 Python 文件继续 version=1，积木作品文件 version=2、mode=3d；积木内部结构也单独携带版本与维度。导入总是新增且不执行；重新生成源码，不沿用源码不一致的预览。
- 跨编辑方式打开作品沿用两侧未保存草稿备份与先落盘后切换；写入失败不切换。新建/打开清空创作会话结果，刷新后重新运行；只有与源码相符的成功快照用于预览与 PNG，导出完整模型不受剖切影响。
- 实现入口：`src/blocks/voxelExamples.ts`、共用的 blocks/model、BlocksEditor、useProgress、useProjectLibrary 和 projects；测试入口 `tests/voxel-blocks.spec.ts`。当前验证结果见开发记录。本轮未执行 Git 提交。

## 2026-09-22 · 三维双画布尺寸对齐修复（已提交 79a9f26）

- 修复了三维工作台右侧上方目标图与下方运行结果画布高度与显示尺寸不一致（上方大、下方被 280px 锁死而偏小偏上）的问题。
- 修改 `src/App.css`：为 `.canvas-frame .voxel-result-wrap` 补齐 `width: 100%; height: 100%; position: relative;`，并将 `.canvas-frame .voxel-viewport` 的 `min-height` 重置为 `0`。
- 修复后上下两画布尺寸完全等高、居中对齐，三维模型缩放比例与投影中心严格对称。

## 2026-09-22 · 三维完全俯视与快捷数格子视角（本轮）

- 放宽三维画布鼠标拖拽与键盘对俯仰角 `pitch` 的硬编码限制至 `[-Math.PI / 2, Math.PI / 2]`（严格 90° 垂直俯仰），支持自由拖拽推平至完全俯视。
- 在顶栏「视图 ▾」菜单中新增「俯视视角（数格子）」按钮：一键切换至 `yaw: 0, pitch: Math.PI / 2`，保留当前缩放；X 轴向右、Z 轴向下，空间立方体规整为纯平面正方形外框，中心虚线与 X/Z 边缘标尺整齐排列，Y 轴自动端视隐去，便于数格子对齐点位。
- 本轮验证：`npm run lint` 通过，`npm run build` 通过，Windows Edge 端到端测试全量 76/76 通过，已目测核查俯视截图 `test-results/voxel-topdown.png`。未执行 Git 提交。


