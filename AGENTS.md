# Pixel Code Lab · Agent 开发约定

## 开始工作

- 使用中文沟通。开始开发前阅读 `docs/交接文档.md`，按任务需要查看代码与其他文档。
- `docs/总体规划.md` 保存长期产品方向；`docs/项目规划书.md` 包含二维 V0.1 历史基线；`docs/开发记录.md` 保存历史变更。不要将历史方案、曾经的阶段限制或长期候选功能直接当作当前待办。
- 用户最新明确指示优先于本文件中的项目约定。需求已明确时直接完成，不重复确认；存在影响实现的关键歧义时先说明理解，再澄清。
- 本文件维护长期工作规则，当前功能状态、提交信息和待办放在交接文档，避免重复维护。

## 工作范围与 Git

- 只实现当前已授权的需求及其必要修复，不自行扩展账号、云端、社区、AI、多语言或课程系统等功能。
- 用户要求时才执行 Git 操作，不在每轮开发中例行提交。一次提交授权不延续到后续修改；用户要求“先提交，再开发”时，提交已有改动后再开发，新改动不自动追加提交。
- 不自行推送、部署、重写提交历史或丢弃已有改动。执行已授权的提交前检查改动范围，保留用户已有工作。
- 不自行启动子 agent；用户明确要求并行或委派时再使用。

## 实现与数据保护

- 技术栈为 React / TypeScript / Vite、Monaco、Pyodide Worker、Canvas。复用现有结构和依赖，避免为小功能引入大规模重构或不必要依赖。
- 保持语言执行、图形数据、渲染与挑战判定相互分离。Python 内部遍历坐标，一次返回结果，不由 JavaScript 逐格调用 Python。
- 保留运行任务编号与过期结果隔离，停止/超时恢复机制，以及真实用户代码错误行号。失败不能伪造新作品或覆盖上次成功结果。
- 保持存档向后兼容。不要覆盖已有代码或损坏存档；改变 schema、关卡标识或字段时考虑旧数据，遵循已有显式恢复流程。
- 不结束无关进程，不为释放端口直接杀进程。优先沿用现有开发地址，避免不同来源导致浏览器存档隔离。

## 产品行为

- 二维挑战按最终像素严格判定，不限制解法；三关全部开放，不自动跳关，不撤销历史通关。不要向学生展示目标公式、参考答案或错误格定位。
- 三维创作允许示例及参考模型；不要将二维挑战的解题限制扩展到三维创作。新增三维判定等玩法须有明确需求。
- 颜色 0 表示空白，不能当作白色；返回值要求为 0～8 的严格 Python 整数，排除 bool、float、None。
- 保持现有科幻终端风格、左侧编辑与右侧上下对照布局。坐标、颜色、悬停提示、高亮与实际显示对象必须一致。
- 三维模型、坐标轴、手柄和命中检测共用一致的投影关系。双图视角与剖切保持同步；剖切只影响观察，不修改原始作品或代码。
- 具体坐标规则、原点移动、存档字段与交互细节以交接文档及当前实现为核对依据；发现不一致时修正文档或报告问题，不擅自改变已确认行为。

## 验证与交付

- 按改动范围验证：静态检查 `npm run lint`，类型与生产构建 `npm run build`，相关行为测试 `npx playwright test <测试文件>`；需要全量回归时使用 `npm test`。
- 修改 UI 后检查真实渲染效果；修改交互或逻辑后验证相关行为。无需为纯文档修改或低影响样式变动编写机械重复的测试。
- 遵循用户针对当前任务明确给出的验证要求；“此前验收按通过处理”不表示后续新功能已经验证。准确区分本次实测与历史记录，不将局部通过描述为全量通过。
- Playwright 使用 Windows Edge；测试服务端口配置见 `playwright.config.ts`。Monaco 多行输入优先用剪贴板粘贴，避免模拟输入叠加缩进造成假失败。
- 功能变化后更新 `docs/开发记录.md`；当前行为、入口、限制变化时同步 `docs/交接文档.md`；阶段范围变化时更新规划书状态说明。保留用户提供的总体规划原文。
- 最终说明完成内容、验证结果和实际限制；执行提交时报告提交号，并说明后续改动是否尚未提交。
